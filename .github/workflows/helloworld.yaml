name: First workflow
on:
  push:
    paths:
      - 'README.md'
      - '!docs/**'
      - '**.js'
    branches:
      - 'main'
      - 'release/**'
      - '!dev'
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo Hello World
      - name: Find the current date
        run: |
          echo "TODAY=$(date +%y-%m-%d)" >> ${GITHUB_ENV}
      - name: Print the current date
        run: echo Hello World, today is $TODAY